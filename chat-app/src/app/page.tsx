import AppSection from "@/components/AppSection";
import React, { Fragment } from "react";

const page = () => {
    return (
        <Fragment>
            <AppSection />
        </Fragment>
    );
};

export default page;
